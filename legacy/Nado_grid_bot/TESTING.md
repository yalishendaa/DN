# Инструкция по проверке работоспособности бота

## ⚠️ ВАЖНО: Безопасность

1. **Используйте реальный приватный ключ** только если у вас есть баланс на Nado mainnet
2. **Начните с минимальных сумм** для тестирования
3. **Всегда проверяйте dry-run** перед реальным запуском
4. **Ордера не отменяются автоматически** при остановке бота — делайте это вручную через Nado UI

---

## Шаг 1: Подготовка

### 1.1 Замените тестовый ключ на реальный

```bash
# Отредактируйте .env
nano .env

# Замените NADO_PRIVATE_KEY на ваш реальный ключ
NADO_PRIVATE_KEY="0xваш_реальный_ключ"
NADO_SUBACCOUNT_NAME="default"
```

### 1.2 Убедитесь, что зависимости установлены

```bash
cd /root/thevse/Nado-grid
source .venv/bin/activate
pip install -r requirements.txt
```

---

## Шаг 2: Проверка подключения (read-only)

```bash
PYTHONPATH=nado-python-sdk:. python test_connection.py
```

**Ожидаемый результат:**
- Подключение к mainnet успешно
- Mark price получен (например, ~$70,000)
- Book info получен (price_increment, size_increment, min_size)
- Позиция и открытые ордера показаны

**Если ошибка:**
- Проверьте, что ключ правильный
- Проверьте интернет-соединение
- Проверьте, что у аккаунта есть доступ к Nado mainnet

---

## Шаг 3: Тест размещения одного ордера (опционально)

⚠️ **Это разместит РЕАЛЬНЫЙ ордер на mainnet!**

```bash
PYTHONPATH=nado-python-sdk:. python test_single_order.py
```

Скрипт:
1. Разместит POST_ONLY buy-ордер на 2% ниже mark price
2. Подождёт 2 секунды
3. Автоматически отменит ордер

**Проверьте:**
- Ордер размещён успешно (digest получен)
- Ордер отменён успешно
- В логах нет ошибок

**Если ошибка `2008 PostOnlyOrderCrossesBook`:**
- Это нормально — цена слишком близко к стакану
- Скрипт попробует с более низкой ценой

---

## Шаг 4: Dry-run (симуляция без реальных ордеров)

```bash
PYTHONPATH=nado-python-sdk:. python -m bot.cli dry-run
```

**Ожидаемый результат:**
- Grid levels сгенерированы (20 buy + 21 sell_target)
- 20 buy-ордеров напечатаны в dry-run режиме
- Цены монотонно убывают (k=-20 → k=-1)
- Все цены кратны `price_increment`
- Summary показывает total_buy_orders=20

**Проверьте логи:**
```json
{"event":"grid_levels_generated","levels_count":41,"buy_levels":20,"sell_targets":21}
{"event":"dry_run_order","k":-1,"side":"buy","price_x18":"...","amount":"..."}
{"event":"dry_run_summary","total_buy_orders":20}
```

---

## Шаг 5: Проверка статуса

```bash
PYTHONPATH=nado-python-sdk:. python -m bot.cli status
```

**Ожидаемый результат:**
```json
{
  "bot_state": "RUNNING",
  "p0": "70735425922624461437131",
  "mark_price_x18": "70749334900636809820103",
  "position_amount_x18": "0",
  "active_orders": 0,
  "pending_buffers": 0,
  "buy_orders": 0,
  "sell_orders": 0
}
```

---

## Шаг 6: Тест с минимальными параметрами (реальный запуск)

⚠️ **Это разместит РЕАЛЬНЫЕ ордера!**

### 6.1 Создайте тестовый конфиг с минимальными параметрами

```bash
cp config.yaml config.test.yaml
```

Отредактируйте `config.test.yaml`:
```yaml
grid:
  grid_step_pct: 0.5        # Больший шаг = меньше ордеров
  levels_down: 3            # Только 3 уровня вниз
  levels_up: 3              # Только 3 уровня вверх
  order_size_x18: 10000000000000000  # Минимальный размер
```

### 6.2 Запустите с тестовым конфигом

```bash
PYTHONPATH=nado-python-sdk:. python -m bot.cli start --config config.test.yaml
```

**Что произойдёт:**
1. Бот подключится к mainnet
2. Получит mark price → сохранит как P0
3. Сгенерирует 3 buy-уровня (k=-3, -2, -1)
4. Разместит 3 POST_ONLY buy-ордера
5. Подпишется на WebSocket fills
6. Будет ждать исполнений и делать refill

**Остановка:**
- Нажмите `Ctrl+C` для graceful shutdown
- Ордера останутся на бирже (не отменяются автоматически!)

### 6.3 Проверьте ордера на бирже

Зайдите в Nado UI и проверьте:
- 3 buy-ордера размещены
- Цены соответствуют ожидаемым (P0 * (1 + k * 0.5%))
- Все ордера POST_ONLY

### 6.4 Тест refill-логики

1. Дождитесь исполнения одного buy-ордера (или разместите рыночный ордер вручную)
2. Бот должен автоматически разместить reduce-only sell на k+1
3. Когда sell исполнится, бот разместит buy на k-1

**Проверьте логи:**
```json
{"event":"fill_received","order_digest":"...","is_taker":false}
{"event":"refill_sell_placed","k_tp":0,"reduce_only":true}
```

---

## Шаг 7: Полный запуск с реальными параметрами

После успешного теста с минимальными параметрами:

```bash
PYTHONPATH=nado-python-sdk:. python -m bot.cli start
```

**Параметры по умолчанию:**
- `levels_down: 20` — 20 buy-уровней
- `levels_up: 20` — 20 sell-targets
- `grid_step_pct: 0.1` — шаг 0.1%
- `order_size_x18: 10000000000000000` — 0.01 BTC

**Это разместит 20 ордеров!** Убедитесь, что у вас достаточно баланса.

---

## Управление ботом

### Пауза (остановить размещение новых ордеров, но слушать fills)

```bash
python -m bot.cli pause
```

### Возобновление

```bash
python -m bot.cli resume
```

### Статус

```bash
python -m bot.cli status
```

### Остановка (graceful shutdown)

```bash
# Отправить SIGTERM процессу
python -m bot.cli stop

# Или нажать Ctrl+C если бот запущен в терминале
```

---

## Мониторинг

### Логи

Логи пишутся в `logs/bot.log` (JSON формат):

```bash
tail -f logs/bot.log | jq
```

### Критические события

Ищите в логах:
- `"level":"CRITICAL"` — taker fill обнаружен → бот автоматически PAUSED
- `"event":"taker_fill_detected"` — нарушение maker-only политики
- `"event":"rate_limit_backoff"` — rate limit, бот делает паузу
- `"event":"ws_connection_error"` — проблемы с WebSocket, переподключение

### База данных состояния

```bash
sqlite3 data/state.sqlite

# Посмотреть активные ордера
SELECT k, side, price_x18, qty_remaining_x18, status FROM orders WHERE status='active';

# Посмотреть fills
SELECT order_digest, filled_qty, remaining_qty, is_taker, ts FROM fills_log ORDER BY ts DESC LIMIT 10;

# Посмотреть pending buffers
SELECT k, side, pending_qty_x18 FROM pending_buffers WHERE CAST(pending_qty_x18 AS INTEGER) > 0;
```

---

## Troubleshooting

### Ошибка: "NADO_PRIVATE_KEY is not set or still placeholder"

→ Замените ключ в `.env` на реальный

### Ошибка: "Product 2 not found in perp_products"

→ Проверьте подключение к mainnet (может быть временная проблема сети)

### Ошибка: "2008 PostOnlyOrderCrossesBook"

→ Нормально для некоторых уровней — бот пропустит их и продолжит

### Ошибка: "2003 OrderAmountTooSmall"

→ Увеличьте `order_size_x18` в конфиге (должен быть >= min_size)

### WebSocket не подключается

→ Бот автоматически переключится на polling fallback (проверьте `polling_enabled: true`)

### Бот не делает refill после fill

→ Проверьте `bot_state` — возможно бот в PAUSED (из-за taker fill или ручной паузы)

---

## Следующие шаги

После успешной проверки:

1. **Настройте параметры** под вашу стратегию в `config.yaml`
2. **Запустите в production** (через systemd, PM2, или screen/tmux)
3. **Настройте мониторинг** (логи, алерты на CRITICAL события)
4. **Регулярно проверяйте** статус и баланс

Удачи! 🚀
